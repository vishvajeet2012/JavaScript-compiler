# JS Compiler v1.0.4

## Added
- Node mode **npm install** bar (Pro) — install packages into local sandbox
- Real `require('package')` for installed npm modules (system Node required)
- Template: npm package (lodash) example
- Website public **What’s New** (`/changelog`) and **Docs** (`/docs`)
- Home **Free vs Pro** comparison table (`#compare`)

## Fixed
- (carried) Promo key activation and Free language locks from 1.0.3

## Changed
- Every release must update public changelog (agent rule V5)

## Removed
- Nothing
